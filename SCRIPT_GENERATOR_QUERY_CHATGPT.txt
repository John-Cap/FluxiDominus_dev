Wait, this debug is not printed at all:
...
  late var flowtrackerTicker =
      Timer.periodic(const Duration(seconds: 1), (timer) {
    print("Timer triggered!");
    flowtracking.notifyListeners();
  });
...