class SomeClass:
    def __init__(self,...):
        ...

    def continuousFetching(self):
    *How to access orgId and labNotebookRef??
        try:
            while not self.pauseFetching:
                self.fetchRecentData_EXAMPLE(*orgID and labNotebookRef as arguments*)
                time.sleep(6)  # Fetch data every 10 seconds
        except KeyboardInterrupt:
            print("Stopped fetching data.")

    def pause(self):
        self.pauseInsertion=True

    def purgeAndPause(self):
        self.dataPoints=[]
        self.pause()

    def start(self,orgId: str,labNotebookRef: str):
        if not (self.insertionThread is None):
            self.pauseInsertion=False
        else:
            
            insertion_thread = threading.Thread(target=self.continuousInsertion)
            fetching_thread = threading.Thread(target=self.continuousFetching,kwargs={"orgId":orgId,"labNotebookRef":labNotebookRef})